<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Job Role Information</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/styles.css">
</head>

<body class="d-flex flex-column min-vh-100">
    <div class="text-bg-light p-3 d-flex align-items-center justify-content-between kainos-font">
        <a href="/" class="d-inline-block">
            <img src="/logos/kainos_logo.png" alt="Kainos Logo" height="80px">
        </a>
        <h1 class="ms-3 mb-0 flex-grow-1 text-end w-100">Job Role Information</h1>
    </div>

    <div class="container my-5 kainos-font">
        <div class="card shadow rounded-4">
            <div class="card-header bg-dark text-white">
                <h2 class="mb-0">
                    <%= role.roleName %>
                </h2>
            </div>
            <div class="card-body">
                <dl class="row">
                    <dt class="col-sm-3">Description</dt>
                    <dd class="col-sm-9">
                        <%= role.description %>
                    </dd>

                    <dt class="col-sm-3">Responsibilities</dt>
                    <dd class="col-sm-9">
                        <%= role.responsibilities %>
                    </dd>

                    <dt class="col-sm-3">SharePoint Job Spec</dt>
                    <dd class="col-sm-9">
                        <% if (role.sharepointUrl) { %>
                            <a href="<%= role.sharepointUrl %>" target="_blank" rel="noopener">View Job Spec</a>
                            <% } else { %>
                                Not available
                                <% } %>
                    </dd>

                    <dt class="col-sm-3">Location</dt>
                    <dd class="col-sm-9">
                        <%= role.location %>
                    </dd>

                    <dt class="col-sm-3">Capability</dt>
                    <dd class="col-sm-9">
                        <%= role.capability && role.capability.capabilityName ? role.capability.capabilityName : '' %>
                    </dd>

                    <dt class="col-sm-3">Band</dt>
                    <dd class="col-sm-9">
                        <%= role.band && role.band.bandName ? role.band.bandName : '' %>
                    </dd>

                    <dt class="col-sm-3">Closing Date</dt>
                    <dd class="col-sm-9">
                        <%= role.closingDate ? new Date(role.closingDate).toLocaleDateString('en-GB') : '' %>
                    </dd>

                    <dt class="col-sm-3">Status ID</dt>
                    <dd class="col-sm-9">
                        <%= role.statusId %>
                    </dd>

                    <dt class="col-sm-3">Open Positions</dt>
                    <dd class="col-sm-9">
                        <%= role.numberOfOpenPositions %>
                    </dd>
                </dl>
            </div>
        </div>
    </div>

    <footer class="text-center py-3 footer text-white mt-auto" id="footer">
        <div class="footer__logo flex justify-content-center">
            <a href="/" class="d-inline-block">
                <img src="/logos/kainos_logo.png" alt="Kainos Logo" height="50px">
            </a>
        </div>
        <p class="mb-0">&copy; Copyright © 2025 Kainos. Registered in NI NI019370</p>
    </footer>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-ENjdO4Dr2bkBIFxQpeoTz1HIcje39Wm4jDKdf19U8gI4ddQ3GYNS7NTKfAdVQSZe"
        crossorigin="anonymous"></script>
</body>

</html>